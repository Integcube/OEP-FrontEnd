export interface OT_FileList {
    fileId: number
    fileName: string
    remarks: string
    path:string
}

export interface PreInsurenceFilterDto{
    Year:number,
    siteId:number,
    userId:number,
    status:number, 
}
